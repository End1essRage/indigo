package storage

import (
	"testing"
)

// setupTestStorage создает временное хранилище для тестов

// TestSimpleCreate базовый тест создания
func TestSimpleCreate(t *testing.T) {
	//fs, cleanup := setupTestStorage(t)
	//defer cleanup()
	/*
		ctx := context.Background()
		collection := "test_collection"

		// Создаем простую map как Entity
		entity := map[string]interface{}{
			"name": "test",
			"data": "test data",
		}

		//id, err := fs.Create(ctx, collection, entity)
		if err != nil {
			t.Fatalf("Create failed: %v", err)
		}

		if id == "" {
			t.Fatal("ID should not be empty")
		}

		t.Logf("Successfully created entity with ID: %s", id)
	*/
}

// TestSimpleGetById тест получения по ID
